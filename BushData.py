def generate_bushrouter_tailscale_apn_ritual(heir_name: str, land_parcel: str, apn_name: str = "BushData") -> str:
    # [FULL CODE AS FETCHED]
    return f"""RITUAL SYNC v0.5.0 — BUSHROUTER + TAILSCALE + APN ..."""